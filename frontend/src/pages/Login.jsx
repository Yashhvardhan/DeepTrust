import React,{useState} from 'react';import {Navigate,useNavigate} from 'react-router-dom';import {login as loginApi,register as registerApi} from '../api.js';
export default function Login({onLogin,token}){const [mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const nav=useNavigate();if(token)return <Navigate to="/" replace/>;
 const submit=async e=>{e.preventDefault();setError('');setLoading(true);try{const {data}=await (mode==='login'?loginApi(email,password):registerApi(email,password));onLogin(data.token,data.user);nav('/')}catch(err){setError(err.response?.data?.error||'Something went wrong')}finally{setLoading(false)}};
 return <div className="auth-page"><div className="auth-left"><div><div className="auth-logo">Deep<b>Trust</b></div><div className="auth-claim">Verify the story.<br/><em>See the evidence.</em></div><p className="auth-copy">An evidence-first verification workspace for news, articles, claims and video.</p></div><div className="auth-sample"><div className="eyebrow light">LIVE VERIFICATION EXAMPLE</div><div className="sample-row"><span className="status fake">LIKELY FAKE</span><span>Bridge collapse clip is from 2019</span><b>18</b></div><div className="sample-row"><span className="status real">LIKELY REAL</span><span>Wholesale inflation eases</span><b>86</b></div><div className="sample-row"><span className="status unclear">UNCLEAR</span><span>Deadline extension claim</span><b>54</b></div></div></div><div className="auth-right"><div className="auth-box"><div className="eyebrow">DEEPTRUST ACCESS</div><h1>{mode==='login'?'Welcome back.':'Create your workspace.'}</h1><p>{mode==='login'?'Sign in to continue to your verification desk.':'Create an account to store verification reports.'}</p>{error&&<div className="error-box">{error}</div>}<form onSubmit={submit}><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><label>Password</label><input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/><button className="primary-btn full" disabled={loading}>{loading?'Working…':mode==='login'?'Sign in':'Create account'}</button></form><button className="switch-auth" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'New here? Create an account':'Already have an account? Sign in'}</button></div></div></div>}
